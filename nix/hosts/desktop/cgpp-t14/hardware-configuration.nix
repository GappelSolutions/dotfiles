{ ... }:

{
  # Replace this file during installation:
  #
  #   nixos-generate-config --root /mnt
  #   cp /mnt/etc/nixos/hardware-configuration.nix \
  #     /mnt/home/cgpp/dev/dotfiles/nix/hosts/desktop/cgpp-t14/hardware-configuration.nix
  #
  # Keep this placeholder tracked so the flake path exists before the first
  # install. It is not intended to describe the real partition layout.
}
