---
name: angular
description: Company-standard Angular 22/Nx/Bun/Spartan skill — signals/DI/routing/HTTP fundamentals plus the Nx vertical-slice/generator-wrapper/Tailwind-v4/zoneless-testing conventions shared by every company Angular project. Use for any Angular task in this repo.
---

# Angular (company standard)

This is the same skill structure checked into every company Angular project — the
architecture (Nx vertical-slice libs, Bun-only tooling, Spartan/Tailwind v4,
generator wrappers, zoneless signal state) is the shared stack, not something
specific to this app. Examples below use this repo's actual names (`apps/gdm-ui`,
`libs/gdm-ui/home`, ...) — substitute this project's own app/domain names, the
pattern is identical everywhere this skill is installed.

## Non-negotiables (read before touching anything)

- **Bun only.** Never `npm install`/`pnpm install`/`npx`. Exact-pinned deps
  (`bunfig.toml`'s `exact = true`) — a version bump edits the pin, doesn't add a range.
- **Never call `ng`/`nx generate` raw for scaffolding a domain, component, or
  service.** This repo's `bun g:*` wrappers bake in directory layout, tags, prefix,
  and i18n scaffolding that the bare generator doesn't know about. See
  [nx-generators.md](references/nx-generators.md).
- **Zoneless.** No `NgZone`, no `fixture.detectChanges()` for anything driven by a
  signal update after the initial render — see [testing.md](references/testing.md).
- A plain non-interactive shell (agent Bash tool included) doesn't load direnv —
  Playwright/devenv-provided env vars need `devenv shell -- <command>` in front.

## Architecture map

```
apps/gdm-ui        thin app shell (bootstrap only) — apps/gdm-ui-e2e is its Playwright suite
libs/ensor/*       @ensor/kit-*  — cross-project shared foundation, prefix esr/hlm
libs/gdm-ui/*       @gdm-ui/*     — app-specific domains, one lib per domain, prefix gdm
```

`libs/ensor/kit-ui` is not one project — it's a directory of independent
per-primitive projects (button, input, field, ...), each generated by
`@spartan-ng/cli:ui`. A domain (either namespace) is never split into
`feature`/`data-access`/`ui`/`util` sub-libs unless it's genuinely big enough to need
independent build/test/lint units.

## Reference index

**Architecture & tooling — this repo's/company's conventions on top of Angular:**

| Task                                                                                                       | Read                                                  |
| ---------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| Scaffolding a domain, component, or service; lib tags & `@nx/enforce-module-boundaries`; i18n scope wiring | [nx-generators.md](references/nx-generators.md)       |
| Adding/styling a `kit-ui` primitive; Tailwind v4 CSS-first setup; theming                                  | [spartan-tailwind.md](references/spartan-tailwind.md) |
| Writing or updating a unit/e2e test                                                                        | [testing.md](references/testing.md)                   |
| State management or forms in a component/service                                                           | [state-forms.md](references/state-forms.md)           |
| Bun/devenv/Nix toolchain quirks, CI parity                                                                 | [toolchain.md](references/toolchain.md)               |

**Angular 22 fundamentals — framework-level, not repo-specific:**

| Task                                                                           | Read                                      |
| ------------------------------------------------------------------------------ | ----------------------------------------- |
| Reactivity: `signal`/`computed`/`linkedSignal`/`effect`/`resource`             | [signals.md](references/signals.md)       |
| Component anatomy, `@if`/`@for`/`@switch`, `input()`/`output()`, host bindings | [components.md](references/components.md) |
| Dependency injection: `inject()`, providers, injection context, hierarchy      | [di.md](references/di.md)                 |
| Defining routes, lazy loading, navigation, guards, resolvers                   | [routing.md](references/routing.md)       |
| Backend calls: `HttpClient`, interceptors, `httpResource`                      | [http.md](references/http.md)             |
| Built-in/custom pipes, pure vs. impure                                         | [pipes.md](references/pipes.md)           |

Full rationale for the architecture/tooling half lives in the root `CLAUDE.md` and
`README.md` — those references are the condensed, task-triggered version of the
same material. The fundamentals half has no repo doc to defer to; it's the primary
source.
