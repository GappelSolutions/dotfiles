#compdef dotnet

# dotnet completion - uses dotnet's built-in completion

_dotnet() {
  local completions=("${(@f)$(dotnet complete "${words}" 2>/dev/null)}")
  if [[ ${#completions[@]} -gt 0 ]]; then
    compadd -a completions
  fi
}

_dotnet "$@"
